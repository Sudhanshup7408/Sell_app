-- Table: public.items

-- DROP TABLE IF EXISTS public.items;

CREATE TABLE IF NOT EXISTS public.items
(
    item_id bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 9223372036854775807 CACHE 1 ),
    brand text COLLATE pg_catalog."default",
    created_at timestamp with time zone,
    delivery_eta_max integer,
    delivery_eta_min integer,
    description text COLLATE pg_catalog."default",
    image_gallery text[] COLLATE pg_catalog."default",
    main_image_url text COLLATE pg_catalog."default" NOT NULL,
    mrp numeric(38,2) NOT NULL,
    pack_size text COLLATE pg_catalog."default" NOT NULL,
    rating_avg double precision,
    rating_count integer,
    selling_price numeric(38,2) NOT NULL,
    stock_status text COLLATE pg_catalog."default" NOT NULL,
    tags text[] COLLATE pg_catalog."default",
    title text COLLATE pg_catalog."default" NOT NULL,
    updated_at timestamp with time zone,
    veg_type text COLLATE pg_catalog."default",
    CONSTRAINT items_pkey PRIMARY KEY (item_id)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.items
    OWNER to postgres;